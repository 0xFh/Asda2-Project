namespace WCell.Constants.LFG
{
    /// <summary>Answer state (Also used to check compatibilites)</summary>
    public enum LFGAnswer
    {
        Pending = -1,
        Deny = 0,
        Agree = 1,
    }
}