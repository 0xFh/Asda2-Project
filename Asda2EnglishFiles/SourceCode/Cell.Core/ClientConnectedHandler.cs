namespace Cell.Core
{
    /// <summary>Handler used for the client connected event</summary>
    /// <param name="client">The client connection</param>
    public delegate void ClientConnectedHandler(IClient client);
}